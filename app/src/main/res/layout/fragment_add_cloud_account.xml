<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/drawer_surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/cloud_choose_provider"
            android:textColor="@color/app_on_surface"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- Google Drive -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGoogleDrive"
            style="@style/Widget.Galerinio.Cloud.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="Google Drive"
            android:textAllCaps="false" />


        <!-- Separator -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/cloud_custom_storage"
            android:textColor="@color/app_on_surface"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- WebDAV -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnWebDav"
            style="@style/Widget.Galerinio.Cloud.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="WebDAV"
            android:textAllCaps="false" />

        <!-- SMB -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSmb"
            style="@style/Widget.Galerinio.Cloud.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="SMB"
            android:textAllCaps="false" />

        <!-- SFTP -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSftp"
            style="@style/Widget.Galerinio.Cloud.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="SFTP"
            android:textAllCaps="false" />

    </LinearLayout>
</ScrollView>
